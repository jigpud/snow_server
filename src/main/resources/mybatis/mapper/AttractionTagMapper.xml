<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.jigpud.snow.mapper.AttractionTagMapper">
    <select id="insertIgnore">
        INSERT IGNORE INTO attraction_tag (attraction_id, tag, tag_md5) VALUES (#{attractionId}, #{tag}, #{tagMd5})
    </select>
</mapper>