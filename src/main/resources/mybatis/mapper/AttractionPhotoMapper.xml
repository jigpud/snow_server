<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.jigpud.snow.mapper.AttractionPhotoMapper">
    <select id="insertIgnore">
        INSERT IGNORE INTO attraction_photo (attraction_id, photo, photo_md5, uploader_id) VALUES (#{attractionId}, #{photo}, #{photoMd5}, #{uploaderId})
    </select>
</mapper>