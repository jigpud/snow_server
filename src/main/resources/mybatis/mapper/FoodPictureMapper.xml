<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.jigpud.snow.mapper.FoodPictureMapper">
    <select id="insertIgnore">
        INSERT IGNORE INTO food_picture (food_id, picture, picture_md5, uploader_id) VALUES (#{foodId}, #{picture}, #{pictureMd5}, #{uploaderId})
    </select>
</mapper>