<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.jigpud.snow.mapper.StoryPictureMapper">
    <select id="insertIgnore">
        INSERT IGNORE INTO story_picture (story_id, picture, picture_md5, uploader_id) VALUES (#{storyId}, #{picture}, #{pictureMd5}, #{uploaderId})
    </select>
</mapper>